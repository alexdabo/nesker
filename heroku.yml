setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  #config:
  #  S3_BUCKET: nesker-bucket
build:
  docker:
    web: Dockerfile
release:
  image: worker
  command:
    - node dist/main

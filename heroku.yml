#setup:
  #addons:
    #- plan: heroku-postgresql
      #as: DATABASE
  #config:
  #  S3_BUCKET: nesker-bucket
build:
  docker:
    web: Dockerfile
    
run:
  web: npm run start:prod

release:
  image: web
  

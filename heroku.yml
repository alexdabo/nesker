#setup:
  #addons:
    #- plan: heroku-postgresql
      #as: DATABASE
  #config:
  #  S3_BUCKET: nesker-bucket
build:
  docker:
    
    context: .
    target: production
    web: Dockerfile
release:
  image: nesker
  command:
    - node dist/main

#setup:
  #addons:
    #- plan: heroku-postgresql
      #as: DATABASE
  #config:
  #  S3_BUCKET: nesker-bucket
build:
  docker:
    release:
       dockerfile: Dockerfile
       target: production
    web: Dockerfile
release:
  image: nesker
  command:
    - node dist/main

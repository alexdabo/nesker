#setup:
  #addons:
    #- plan: heroku-postgresql
      #as: DATABASE
  #config:
  #  S3_BUCKET: nesker-bucket
build:
  docker:
    web: Dockerfile
release:
  image: nesker
  target: production
  command:
    - node dist/main
